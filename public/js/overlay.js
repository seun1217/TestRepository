// 툴팁 오버레이 렌더링. docs/CONTRACT.md 4절 overlay.js. 구현은 워크플로우 에이전트가 채운다.
export function renderOverlay(container, plants, { videoEl, onSelect }) {}

export function clearOverlay(container) {
  container.replaceChildren();
}
